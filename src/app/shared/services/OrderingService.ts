import { Injectable } from "@angular/core";
import { Product } from "~/app/shared/models/product.model";

@Injectable({
    providedIn: "root"
})
export class OrderingService {

    CalculateSummary(products: Array<Product>) {
        
    }
}
